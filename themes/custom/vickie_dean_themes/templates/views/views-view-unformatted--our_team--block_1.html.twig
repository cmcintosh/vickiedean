<div class="text-center">
      <h3>OUR TEAM</h3>
      <div class="row">
          {% for row in rows %}
            {%
              set row_classes = [
                default_row_class ? 'views-row',
              ]
            %}

              {{ row.content }}

          {% endfor %}
      </div>
</div>