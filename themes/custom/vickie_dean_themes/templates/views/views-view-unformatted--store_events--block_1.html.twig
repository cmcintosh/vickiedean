    <div class="text-center">
      <h3>Store Events</h3>
      <div class="row">
          {% for row in rows %}
            {%
              set row_classes = [
                default_row_class ? 'views-row',
              ]
            %}

              {{ row.content }}

          {% endfor %}
      </div>
    </div>